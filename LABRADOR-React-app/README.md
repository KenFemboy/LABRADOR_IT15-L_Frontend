How to run Frontend
    1. Ensure you have already installed NodeJS
    2. Change your Directory to Labrador-React-App (In the terminal run "cd Labrador-React-App")
    3. Run "Npm Install" in the terminal (Make sure you're in the Labrador-React-App Directory)
    4. Wait for the installation to finish then run ("npm run dev)
    5. Open your browser and type "localhost:5173" in the url or CRTL+Left Click the url provided by react in the terminal
    6. Make sure the backend is running (Refer to Labrador_IT16-L_Backend)

    The Example Credentials for Admin is:
    email: Test@example.com 
    password: password

    In order to log in as a student:
    1. Open your database (Laragon/Xampp)
    2. Find a student and copy their email address and student_number
    3. User the email address and student_number (type in Student number, ignore password field)